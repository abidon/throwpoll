# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET		/				controller.Application.showPollCreator()
GET		/creator		controller.Application.showPollCreator()
POST	/creator		controller.Application.saveNewPoll()
GET		/poll/:qid		controller.Application.showVoteInterface(qid)

GET		/vote/:qid		controller.Application.voteForQuestion(qid)


# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
